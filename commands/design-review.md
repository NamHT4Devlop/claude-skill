---
description: Review UI/UX + accessibility of a running app (browser screenshots) or frontend code — consistency, hierarchy, responsive, a11y; findings + fixes
argument-hint: <app URL> or frontend path/components
---

Use the **namht-design-review** skill to review the UI below: if a URL, drive the Claude-in-Chrome
MCP to screenshot key screens (desktop + mobile) and review visual consistency, layout/hierarchy,
responsive, accessibility (WCAG: contrast/alt/labels/keyboard/ARIA), microcopy, and interaction
states — against the project design system (KB) if present. Cite each screen/component + screenshot,
give severity + the concrete fix. Read-only; hand fixes to /namht-build.

Target (URL or frontend path):
$ARGUMENTS
